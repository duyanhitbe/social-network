query GetAllUser {
  getAllUser {
    data {
      id
      name
      avatar
    }
    pagination {
      limit
      page
      total
    }
  }
}

query UserGetMe {
  userGetMe {
    id
    name
    username
    email
    avatar
  }
}